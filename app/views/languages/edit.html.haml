%h1.heading
  = "Editing language %s" % @language.id
  
- semantic_form_for(@language) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', languages_path