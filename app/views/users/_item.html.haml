- content_tag_for(:tr, user, :class => cycle(:odd, :even)) do
  %td.username= h user.try(:username)
  - if logged? && current_user.admin?
    %td.actions
      = link_to 'Show', user_path(user)
      |
      = link_to 'Edit', edit_user_path(user)
