%h1.heading
  = "Editing user profile of %s" % @user.username
  
- semantic_form_for(@user) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', users_path
