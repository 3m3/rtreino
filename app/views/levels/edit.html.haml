%h1.heading
  = "Editing level %s" % @level.id
  
- semantic_form_for(@level) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', levels_path