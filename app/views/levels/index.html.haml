%h1.heading
  = "Levels"
  
%p.actions
  = link_to 'New Level', new_level_path
  
%table
  %thead.header
    %tr
      %th.name= 'Name'
      %th.actions= 'Actions'
  %tbody.items.levels
    - @levels.each do |level|
      = render 'item', :level => level
      
= will_paginate(@levels)
