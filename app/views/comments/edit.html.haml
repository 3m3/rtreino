%h1.heading
  = "Editing comment %s" % @comment.id
  
- semantic_form_for(@comment) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    = link_to_function "Preview", "preview('comment_body')"
    |
    = link_to 'Cancel', comments_path

%div#preview
