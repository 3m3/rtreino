<h1>Editing problem</h1>

<% form_for(@problem) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f, :bt_submit => 'Update' } %>
<% end %>

<%= link_to 'Show', @problem %> |
<%= link_to 'Back', problems_path %>
