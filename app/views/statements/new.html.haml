%h1.heading
  = 'New Statement'
  
- semantic_form_for([@problem, @statement]) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Create'
    = form.commit_button 'Create and continue'
    = link_to 'Back to problem', problem_path(@problem)
