- form.inputs do
  = form.input :creator_id, :as => :hidden, :value => (@statement.creator_id || current_user.id)
  = form.input :updater_id, :as => :hidden, :value => current_user.id
  = form.input :language_id, :label => 'Language', :label_method => :name
  = form.input :title, :label => 'Title'
  = form.input :body, :label => 'Body'

:javascript
  document.observe('dom:loaded', function() {
    setToolbarFor('statement_body');
  });
